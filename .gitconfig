[user]
  name = mgrsskls
  email = mail@mgrossklaus.de
[push]
  default = upstream
[alias]
  co = checkout
  pf = push --force
  pl = pull
  s = stash
  sp = stash pop
  plr = pull --rebase
[core]
  excludesfile = ~/.gitignore
  editor = /usr/bin/vim
  pager = diff-so-fancy | less --tabs=4 -RFX
